@page
@model Proiect.Pages.Specializations.IndexModel

@{
    ViewData["Title"] = "Specializations";
}

<h1>Specializations</h1>

<p>
    <a asp-page="Create" class="btn btn-outline-danger">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Specialization[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Specialization[0].Description)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Specialization) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                <div class="btn-group" role="group">
                    <a asp-page="./Edit" asp-route-id="@item.ID" class="btn btn-outline-danger">Edit</a> 
                    <a asp-page="./Details" asp-route-id="@item.ID" class="btn btn-outline-dark">Details</a> 
                    <a asp-page="./Delete" asp-route-id="@item.ID" class="btn btn-outline-danger">Delete</a>
                </div>
            </td>
        </tr>
}
    </tbody>
</table>
